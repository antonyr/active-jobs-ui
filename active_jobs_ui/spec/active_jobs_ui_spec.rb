require "spec_helper"

describe ActiveJobsUi do
  it "has a version number" do
    expect(ActiveJobsUi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
